type: charm
bases:
  - build-on:
      - name: ubuntu
        channel: "22.04"
        architectures:
          - amd64
    run-on:
      - name: ubuntu
        channel: "22.04"
        architectures:
          - amd64
      - name: ubuntu
        channel: "24.04"
        architectures:
          - amd64
parts:
  include:
    plugin: dump
    source: .
    prime:
      - actions
      - actions.yaml
      - hooks
      - manifest.yaml
      - metadata.yaml

